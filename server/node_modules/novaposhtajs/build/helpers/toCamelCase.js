"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function toCamelCase(x, format) {
    const fn = format === "upper"
        ? "toUpperCase" :
        format === "lower"
            ? "toLowerCase" :
            undefined;
    if (fn === undefined)
        throw new Error("format must be `lower` or `upper`");
    if (typeof x === "string") {
        if (x.length == 0)
            return x;
        if (x.length == 1)
            return x[fn]();
        return x[0][fn]() + x.slice(1);
    }
    if (typeof x === "object") {
        const tmp = {};
        if (Array.isArray(x)) {
            return x.map((item) => toCamelCase(item, format));
        }
        else {
            for (const key in x) {
                const ukey = toCamelCase(key, format);
                if (typeof x[key] === "object") {
                    tmp[ukey] = toCamelCase(x[key], format);
                }
                else {
                    tmp[ukey] = x[key];
                }
            }
        }
        return tmp;
    }
    return x;
}
exports.default = toCamelCase;
